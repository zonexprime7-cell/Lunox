import { RainlinkPlugin as Plugin } from 'rainlink';
import { Rainlink } from 'rainlink';
import { RainlinkPluginType } from 'rainlink';
import { RainlinkNode } from 'rainlink';
import { VoiceChannelOptions } from 'rainlink';
import { RainlinkWebsocket } from 'rainlink';
import { RainlinkDatabase } from 'rainlink';
export declare class RainlinkPlugin extends Plugin {
    protected manager: Rainlink | null;
    protected runningWs: RainlinkDatabase<RainlinkWebsocket>;
    constructor();
    /** Name function for getting plugin name */
    name(): string;
    /** Type function for diferent type of plugin */
    type(): RainlinkPluginType;
    /** Open the ws voice reciver client */
    open(node: RainlinkNode, voiceOptions: VoiceChannelOptions): void;
    /** Open the ws voice reciver client */
    close(guildId: string): void;
    protected wsMessageEvent(node: RainlinkNode, data: string): void;
    /** Load function for make the plugin working */
    load(manager: Rainlink): void;
    /** unload function for make the plugin stop working */
    unload(): void;
    protected debug(logs: string): void;
}
//# sourceMappingURL=plugin.d.ts.map