"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RainlinkPlugin = void 0;
const rainlink_1 = require("rainlink");
const rainlink_2 = require("rainlink");
const rainlink_3 = require("rainlink");
const rainlink_4 = require("rainlink");
const rainlink_5 = require("rainlink");
class RainlinkPlugin extends rainlink_1.RainlinkPlugin {
    constructor() {
        super();
        this.manager = null;
        this.runningWs = new rainlink_5.RainlinkDatabase();
    }
    /** Name function for getting plugin name */
    name() {
        return 'rainlink-voiceReceiver';
    }
    /** Type function for diferent type of plugin */
    type() {
        return rainlink_2.RainlinkPluginType.Default;
    }
    /** Open the ws voice reciver client */
    open(node, voiceOptions) {
        if (!this.manager)
            throw new Error('This plugin is unloaded!');
        const wsUrl = `${node.options.secure ? 'wss' : 'ws'}://${node.options.host}:${node.options.port}`;
        const ws = new rainlink_4.RainlinkWebsocket(wsUrl + '/connection/data', {
            headers: {
                authorization: node.options.auth,
                'user-id': this.manager.id,
                'client-name': `${rainlink_3.metadata.name}/${rainlink_3.metadata.version} (${rainlink_3.metadata.github})`,
                'user-agent': this.manager.rainlinkOptions.options.userAgent,
                'guild-id': voiceOptions.guildId,
            },
        });
        this.runningWs.set(voiceOptions.guildId, ws);
        ws.on('open', () => {
            var _a;
            this.debug('Connected to nodelink\'s voice receive server!');
            (_a = this.manager) === null || _a === void 0 ? void 0 : _a.emit("voiceConnect", node);
        });
        ws.on('message', (data) => this.wsMessageEvent(node, data));
        ws.on('error', err => {
            var _a;
            this.debug('Errored at nodelink\'s voice receive server!');
            (_a = this.manager) === null || _a === void 0 ? void 0 : _a.emit("voiceError", node, err);
        });
        ws.on('close', (code, reason) => {
            var _a;
            ws.removeAllListeners();
            this.debug(`Disconnected to nodelink's voice receive server! Code: ${code} Reason: ${reason}`);
            (_a = this.manager) === null || _a === void 0 ? void 0 : _a.emit("voiceDisconnect", node, code, reason);
        });
    }
    /** Open the ws voice reciver client */
    close(guildId) {
        if (!this.manager)
            throw new Error('This plugin is unloaded!');
        const targetWs = this.runningWs.get(guildId);
        if (!targetWs)
            return;
        targetWs.close(1006, 'Self closed');
        this.runningWs.delete(guildId);
        this.debug('Destroy connection to nodelink\'s voice receive server!');
        return;
    }
    wsMessageEvent(node, data) {
        var _a, _b;
        if (!this.manager)
            throw new Error('This plugin is unloaded!');
        const wsData = JSON.parse(data.toString());
        this.debug(String(data));
        switch (wsData.type) {
            case 'startSpeakingEvent': {
                (_a = this.manager) === null || _a === void 0 ? void 0 : _a.emit("voiceStartSpeaking", node, wsData.data.userId, wsData.data.guildId);
                break;
            }
            case 'endSpeakingEvent': {
                (_b = this.manager) === null || _b === void 0 ? void 0 : _b.emit("voiceEndSpeaking", node, wsData.data.data, wsData.data.userId, wsData.data.guildId);
                break;
            }
        }
    }
    /** Load function for make the plugin working */
    load(manager) {
        this.manager = manager;
        this.manager.on('playerCreate', player => {
            if (!player.node.driver.id.includes('nodelink'))
                return;
            this.open(player.node, {
                guildId: player.guildId,
                shardId: player.shardId,
                voiceId: player.voiceId || '',
                textId: player.textId,
            });
        });
        this.manager.on('playerDestroy', player => {
            if (!player.node.driver.id.includes('nodelink'))
                return;
            this.close(player.guildId);
        });
    }
    /** unload function for make the plugin stop working */
    unload() {
        var _a, _b;
        (_a = this.manager) === null || _a === void 0 ? void 0 : _a.removeListener('playerCreate', () => { });
        (_b = this.manager) === null || _b === void 0 ? void 0 : _b.removeListener('playerDestroy', () => { });
        this.manager = null;
    }
    debug(logs) {
        this.manager
            ? this.manager.emit(rainlink_2.RainlinkEvents.Debug, `[Rainlink] / [Plugin] / [Voice Receiver] | ${logs}`)
            : true;
    }
}
exports.RainlinkPlugin = RainlinkPlugin;
